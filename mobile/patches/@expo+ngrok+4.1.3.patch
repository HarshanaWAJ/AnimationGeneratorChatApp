diff --git a/node_modules/@expo/ngrok/src/client.js b/node_modules/@expo/ngrok/src/client.js
index 3f748a3..8ecfc42 100644
--- a/node_modules/@expo/ngrok/src/client.js
+++ b/node_modules/@expo/ngrok/src/client.js
@@ -29,17 +29,19 @@ class NgrokClient {
     } catch (error) {
       let clientError;
       try {
-        const response = JSON.parse(error.response.body);
+        const responseBody = error.response ? error.response.body : null;
+        const response = responseBody ? JSON.parse(responseBody) : { msg: error.message || "Unknown ngrok error" };
         clientError = new NgrokClientError(
           response.msg,
           error.response,
           response
         );
       } catch (e) {
+        const fallbackBody = error.response ? error.response.body : error.message;
         clientError = new NgrokClientError(
-          error.response.body,
+          fallbackBody,
           error.response,
-          error.response.body
+          fallbackBody
         );
       }
       throw clientError;
@@ -52,7 +54,8 @@ class NgrokClient {
         (response) => response.statusCode === 204
       );
     } catch (error) {
-      const response = JSON.parse(error.response.body);
+      const responseBody = error.response ? error.response.body : null;
+      const response = responseBody ? JSON.parse(responseBody) : { msg: error.message || "Unknown ngrok error" };
       throw new NgrokClientError(response.msg, error.response, response);
     }
   }
